## Learned User Preferences

- Do not modify `models.py` or `grid_data_prep.py`; wrap them with new service code instead.
- Keep superseded circuit-level modules (`data_prep.py`, `main.py`, `prep_hrrr.py`) for reference only; do not build new work on them.
- Treat sibling `dataset_demo/` as read-only reference; never modify or commit there: new code goes only in Wildfire Services.
- Prefer a reviewable plan before applying structural or service changes; use separate commits for baseline, restructure, and new service code.
- Verify data by content (e.g. NetCDF time coordinates), not filenames; this project has a history of filename-vs-content mismatches.
- Prefer failing verification loudly over adjusting tests or expectations to pass; ground-truth against source files or SQL. For visual work, verify against the rendered result (screenshots), not programmatic element/property checks; if rendering cannot be inspected, mark unverified rather than reporting it done. Distinguish live Ask verification from canvas-API render-then-screenshot checks.
- If fitted risk params are missing, fail with a clear message rather than auto-fitting; pause for human verification of adjacency rebuild stats (nnz / average neighbors) before running model fits.
- When fitted coefficients look scientifically implausible, audit covariates before refitting.
- Prefer dropping corrupt weather days over substituting a different hour-of-day HRRR export when that would introduce a systematic temperature offset.
- Do not overclaim small OOS log-likelihood gaps; quantify uncertainty (e.g. day-blocked bootstrap) and treat noise-scale ΔLL as a tie.
- Document durable scientific findings and data caveats in the README.
- After each Historical Map canvas slice, confirm the Planning Tool tab still renders plot images before calling the slice done. Serve with `python frontend/serve.py` (not `python -m http.server` from `frontend/`); canvas CSS/JS stays scoped to `#sfps-tab-historical` / `#historical-canvas-host`. Planning Tool is out of scope for canvas work. StatCard is a 1–3 card strip (do not stretch a single card across the canvas); SpatialContext lays Coordinates, IOU, HFTD, county, and grid cell horizontally; Comparison, TimeSeries, and RecordTable stay full width; the map fills the canvas and the Leaflet instance stays alive when collapsed; idle map filters grey when a non-map answer is showing. Working reference: [`frontend/CANVAS.md`](frontend/CANVAS.md).

## Learned Workspace Facts

- Repo is Wildfire Services (personal `origin`: `ByteMasterMike/Wildfire-Services`; team repo is `Woody-Zhu-Group/Wildfire-Platform` on the `platform` remote, and pushes go to `platform`) with `services/{risk_forecasting,data_query,visualization,comparison,agent,shared}/`, `website/` (the current React/TypeScript workspace, built into `docs/`), `frontend/` (the older local Historical Map and Planning Tool), `shared/`, `db/`, `deploy/`, and `tests/`; risk service wraps the grid cNHPP ignition model. On Windows, run modules with repo-root `PYTHONPATH` and `PYTHONIOENCODING=utf-8` so `models.py` console output does not crash on ξ characters.
- PostGIS warehouse: `db/schema.sql` + `python -m db.loaders` (Docker Compose PostGIS 16 on host port **5433**). DB settings in `shared/db.py` (loads repo `.env`). Source map data is read-only from sibling `dataset_demo/assets/data`, except HFTD tier and IOU territory polygons, which load from cached CPUC FeatureServer Esri JSON in `data/boundaries/` behind a validity and area gate (PR #45, `docs/DATA_CHANGE_HFTD_IOU.md`). Circuit IDs are TEXT 9-digit zero-padded, never numeric. No FKs from EPSS/PSPS to circuits; orphans reported at load + validation (17 PSPS circuit IDs lack GNA geometry). No CPZ polygons in dataset_demo (known gap; HFTD Tier 2/3 only). CPUC ignitions get a `county` column at load via point-in-polygon against Census TIGER California county polygons (`wildfire.counties`); `/spatial/point` returns that county.
- Data query API: `uvicorn services.data_query.app:app --reload --app-dir .`, county, utility, and tier filters are normalized in `services/data_query/filters.py` (county names via `services/shared/counties.py`; "Butte County" resolves to `Butte`, an unknown county is a 400 with close matches, never an empty result; the visualization and comparison services use the same parsers, and the agent tool layer normalizes county arguments before the call and shows the backend's suggestions on `unknown_county`). CAL FIRE defaults to `incident_type in (Wildfire, Fire)` excluding nulls; use `incident_type=untyped` / `utility=untagged`. EPSS `outage_type`/`cause` and an explicit CAL FIRE `incident_type` resolve against stored warehouse values (`services/shared/stored_values.py`, case-insensitive; an unknown value is a 400). EPSS cause codes fold into their word form under a written rule (`EPSS_CAUSE_CODE_WORDS` in `services/shared/naming.py`, exported by `services/shared/dataset_registry.py`: VEG, UNK, 3RD; EF is ambiguous and left alone): filters and groupings match both spellings and results show the word. A CAL FIRE county filter, ranking, grouping, or comparison counts a multi-county incident ("Shasta, Tehama") in every listed county (`services/shared/calfire_county.py`); county-scoped CAL FIRE meta carries `multi_county_incidents` and the agent adds the `calfire_multi_county` caveat. Leading-zero circuit test ID: `043371102` (inventory example `012041102` is absent). Verification: `python tests/report_results.py` (requires data_query :8000 and risk :8001); EPSS loader drops 1 exact duplicate in 2024 (raw CSV 2788 → loaded 2787).
- Visualization API: `uvicorn services.visualization.app:app --port 8002 --app-dir .`, EPSS map-layer returns circuit lines (`include_outages=true` embeds outage rows); `/event-detail` for `circuits` returns `outages`, for `psps` returns `affected_circuits`. CORS enabled. Ignitions: attribute `utility=` ≠ spatial `/spatial/summary` (PGE 2024: 532 vs 536). Frontend is a slim copy without `website_plots`; Historical Map uses visualization `:8002`, HDWI stays static, and CAL FIRE uses the verified Wildfire/Fire default and stays red `#b91c1c` (one hue per dataset; magnitude via size or opacity). Local UI: `python frontend/serve.py` on **8765** (mounts sibling `dataset_demo` at `/dataset_demo/`); plain `http.server` from `frontend/` 404s Planning Tool plots.
- National ignitions table `wildfire.us_ignitions` (~33,457 rows after extract): source `data/north_america/Wildfire_Dataset.csv` + extract gitignored; data query `GET /us-ignitions`; visualization `dataset=us_ignitions` is an off-by-default toggle with conditional CONUS zoom, drawn teal `#0f766e` in the older `frontend/` and red `#dc2626` in `website/`. FireCastRL is an event-window classification sample with synthetic controls, not a cell-day census suitable for cNHPP training; it is all-cause, not comparable to CPUC, and California-heavy (≈40% overall / ≈59% of 2024 by state PIP).
- Comparison API: `uvicorn services.comparison.app:app --port 8003 --app-dir .`: `/compare-utilities`, `/compare-regions`, `/compare-periods`. EPSS non-PGE → null + reason (never zero); missing bars need a visible hatched placeholder, not an empty axis plus floating “no data”. Default ignition definition attribute for utilities, spatial for HFTD.
- Agent prototype: `uvicorn services.agent.app:app --port 8004 --app-dir .`, deterministic routing precedes seven grouped OpenAI-compatible tools (`data_query_records`, `data_query_rank`, `data_query_spatial`, `visualization_create`, `visualization_inspect`, `risk_forecast`, `comparison_run`); `risk_surface` and `risk_metrics` (model performance from `GET /metrics`, rule `risk_model_metrics`) are router-only. The model tier is OpenRouter only (GPT-6 Luna, GPT-6 Sol on retries; defaults in `services/agent/config.py`, overridable via `AGENT_LLM_MODEL` and `AGENT_LLM_FALLBACK_MODEL`); it needs `OPENROUTER_API_KEY` and `AGENT_ALLOW_REMOTE_PROVIDER=true` or startup fails. The local Ollama/qwen path and the GPU control service were removed on 2026-09-24. The Jev decision layer (TypeSafe, a non-generative typed-decision model with probabilities; see CLAUDE.md) lives under `services/agent/decisions/` (`jev_policy.py`, `backend.py`, `v3.py`). `AGENT_JEV_MODE` accepts `off` (default), `shadow`, `tool_pick`, `tool_pick_template`, and `decide` (router backstops first, then Jev's disposition behind a 0.8 decline gate and a 0.9 answer gate; Jev owns the disposition, the router owns the clarification or refusal wording; `docs/JEV_DECIDE.md`); plan mode was archived on the `jev-plan-archive` branch and is not accepted (`docs/JEV_MULTI_TOOL.md`). `AGENT_SLOT_PLAN` (off by default) plans multi-entity questions from router slots; with decide on, decide runs first. `AGENT_JEV_BACKEND` is `typesafe` (default) or `openrouter` (see `docs/OPENROUTER.md`). Location phrasing (`where` / `see where` / `locations of`) routes to a map, even when the question also sounds like a count. Year-scoped geographic counts keep the Map on the canvas (same filters, no extra tool); partial-year counts, risk, and spatial summaries stay map-less. Caveats attach after any successful tool path (path-independent). Every utility ignition count gets a same-period attribute/spatial companion comparison. Model-path filter grounding drops schema-valid enum values the question never asked for (`utility=untagged` unless the question mentions untagged or unattributed records; CAL FIRE `incident_type_mode` `all` or `untyped` unless it asks for every type or untyped records), and injected eval faults record the arguments that would have run. An eval case may list `accepted_tool_sequences` with an `expectation_note` when more than one tool sequence is correct. Relative dates resolve in the harness against the current date (never the model); a deterministic rule must not fire if the question has filters the rule cannot express. Frontend Ask panel uses SSE `POST /ask/stream`; leave `POST /ask` unchanged for eval. Do not add a `render_view` tool (canvas views are harness-planned). The eval runner defaults to the production model (openai/gpt-6-luna) and spends OpenRouter credits; run it only with an explicit budget. Eval files on main: cases.json (107), jev_paraphrases.json (41), holdout v1 (`jev_holdout.json`), holdout v2 (`jev_holdout_v2.json`), and the v3 questions (`jev_holdout_v3_questions.json`, labels in `jev_holdout_v3_labels_chatgpt.json`); v3 is frozen. Routing sends `tool_choice: "required"`; the runner still tracks no-tool/direct-answer attempts.
- Fitted params live at `services/risk_forecasting/artifacts/cnhpp_params.npz` (committed in `6b58596`, ~2KB); risk API runs on :8001. California grid has 824 cells at 0.24° spacing; rebuilt adjacency should have nnz ≈ 3922 and ~3.8 average neighbors.
- Covariate order is TMP, SPFH, wind_speed, NDVI, fm100, plus an intercept; single-date prediction needs a trailing covariate window because of the spatial memory term. Do not call `grid_data_prep.load_year()` (broken signature); fit/predict wrappers call the year loaders directly and concatenate per-year `events_YYYY.csv` files.
- Dec 2–31 2020 weather was corrupt in `California_HRRR_daily_2020_01.csv` (TMP held SPFH-scale values); those 30 days are dropped, and `prep_hrrr_grid.py` rejects days whose median TMP is outside 200–330 K.
- Grid cell 461 has all-NaN NDVI/fm100 across vegetation NetCDFs; cells 71, 439, and 521 are all-NaN for fm100 only (mean-filled in wrappers).
- Positive SPFH coefficient is a covariate-semantics issue (SPFH is not dryness; eventual fix is VPD or RH); fm100 is largely redundant with TMP (train correlation ≈ −0.64). On the corrected 824-cell weather grid, cNHPP vs NHPP OOS ΔLL is a statistical tie (sign flips across years; CIs cover 0).
